# Case-to-Leaf attribution boundary

This directory is reserved for reviewed, versioned Case-to-Leaf attribution
maps. Phase 1 defines the prototype contract only and intentionally performs
no historical backfill.

Attributions must reference an exact Case version and node, a stable Leaf
Skill, an exact rubric version, and an explicit reviewer.
